<div id="product-<%= product.id %>">
  <h3>
    <% if current_user %>
      <%= link_to product.name, new_product_users_product_path(product), class: "product", data: {id: product.id} %>
    <% else %>
      Name: <%= product.name %>
    <% end %>
  </h3>
  <h3>
    Category: <%= product.category %>
  </h3>
  <p>
    Serving size: <%= product.serving_size %><%= product.serving_unit %>
  </p>
</div>
